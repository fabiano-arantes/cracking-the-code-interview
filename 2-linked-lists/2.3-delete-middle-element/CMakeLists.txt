CMAKE_MINIMUM_REQUIRED(VERSION 3.0)
PROJECT(delmidelement C)

SET(BASE_DIR "${CMAKE_CURRENT_SOURCE_DIR}")

ADD_DEFINITIONS("-DDEBUG")

ADD_EXECUTABLE("${PROJECT_NAME}" ${BASE_DIR}/${PROJECT_NAME})
TARGET_LINK_LIBRARIES(${PROJECT_NAME} utilcc)	
	
