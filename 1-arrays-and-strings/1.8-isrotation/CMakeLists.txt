CMAKE_MINIMUM_REQUIRED(VERSION 3.0)
PROJECT(isrotation C)

ADD_DEFINITIONS("-DDEBUG")

SET(CMAKE_C_FLAGS "-g -O0 -Wall")

ADD_EXECUTABLE("${PROJECT_NAME}" ${CMAKE_CURRENT_SOURCE_DIR}/${PROJECT_NAME})
TARGET_LINK_LIBRARIES(${PROJECT_NAME} utilcc)	
